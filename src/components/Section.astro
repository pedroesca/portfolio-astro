---
const { title } = Astro.props;
---

<section>
  {title && <h2>{title}</h2>}
  <slot />
</section>

<style>
  section {
    @apply max-w-3xl m-auto;
  }

    h2 {
        @apply mb-2 font-bold leading-6 text-2xl;
    }
</style>
